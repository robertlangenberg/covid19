name: Build page
on:
  push:
    branches:
      - main
  schedule:
     - cron:  '4 */6 * * *'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Build
        run: |
          python3 -m venv venv
          source venv/bin/activate
          pip3 install -r requirements.txt
          jupyter nbconvert --execute --to html Covid.ipynb
          mkdir public
          mv *.html public
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v3
        with:
          personal_token: ${{ secrets.PERSONAL_TOKEN }}
